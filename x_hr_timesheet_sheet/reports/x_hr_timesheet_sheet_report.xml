<?xml version="1.0" encoding="utf-8"?>
<odoo>
<!--
    QWeb Reports
    id : the generated record's external id
    name (mandatory)    : Template Name : only useful as a mnemonic/
                          description of the report when looking for
                          one in a list of some sort
    model (mandatory)   : the model your report will be about
    report_type (mandatory) : either 'qweb-pdf' for PDF reports or 'qweb-html'
                            for HTML
    report_name         : the name of your report (which will be the name of
                          the PDF output)
    groups              : Many2many field to the groups allowed to view/use
                          the current report
    attachment_use      : if set to True, the report will be stored as an
                          attachment of the record using the name generated by
                          the attachment expression; you can use this if you
                          need your report to be generated only once (for legal
                          reasons, for example)
    attachment          : python expression that defines the name of the report;
                          the record is acessible as the variable object
    paperformat         : external id of the paperformat you wish to use
                          (defaults to the company's paperformat if not specified)
    file                : module_directory_name.xml_template_id
-->
        <report id="x_hr_timesheet_sheet_weekly_pdf_report"
            model="hr_timesheet_sheet.sheet"
            string="Weekly Timesheet"
            report_type="qweb-pdf"
            name="x_hr_timesheet_sheet.x_timesheet_weekly_report_template"
            file="x_hr_timesheet_sheet.x_timesheet_weekly_report_template"
        />
</odoo>
